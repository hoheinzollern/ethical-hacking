mini #1:

mov rax, rdi
mov ebx, DWORD PTR[r8]
mov DWORD PTR[r9], esi

mini #2:

add rax, rdi
add rax, rsi
mov bx, WORD PTR[r8]
imul rbx, rdx
sub rax, rbx
mov QWORD PTR[r9], rax

mini #3:
loop:
mov al, BYTE PTR[r8]
cmp al, 0x50
jle else
sub al, 0x37
jmp join
else:
add al, 0x13
join:
mov BYTE PTR[r9], al
inc r8
inc r9
inc rbx
cmp rbx, rdi
jne loop


